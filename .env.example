# Production Gemini API Key (Keep secure on server)
GEMINI_API_KEY=

# Optional: Port for the backend server (Defaults to 3000)
PORT=3000

# Optional: Frontend API URL (leave blank for same-origin)
VITE_API_URL=
